# 레이드 최적화 설계

## 목표

이 계산기의 목표는 수학적 최적값을 끝까지 증명하는 것이 아니라, **30초 안에 실전에서 충분히 좋은 전역 계획을 안정적으로 찾는 것**이다.

우선순위:
1. 가능한 한 높은 라운드까지 진행한다.
2. 최종보스 도달 시 최종보스 딜을 높인다. 미도달 시 마지막 도달 라운드 유효 딜을 높인다.
3. 비슷한 목표 딜 범위에서는 앞 라운드의 과도한 낭비와 실제 오버딜을 줄인다.
4. 유저가 상대적으로 잘 치는 속성, 희소한 속성/니케, 공격권과 미래 선택지를 함께 고려한다.
5. 작은 수치 차이의 최적성 증명보다 큰 구조적 개선을 우선한다.

일반 보스는 잔여 HP 10억 이하를 계획상 클리어로 인정한다.

## 파이프라인

### 1. 자원 분석

각 유저/파티 후보에 대해 다음 정보를 계산한다.
- 레벨별 공격력 보정 후 같은 속성 중앙값 대비 상대 효율
- 해당 속성을 칠 수 있는 유저 수(속성 희소성)
- 파티 니케가 다른 후보에서 얼마나 자주 쓰이는지(니케 희소성)
- 남은 공격권 및 이미 사용한 니케

효율은 탐색 우선순위에만 사용하며 후보를 삭제하거나 고정하지 않는다.

### 2. Beam Search 초기해

라운드와 보스를 순서대로 채우면서 실행 가능한 계획을 만든다.

평가 요소:
- `HP - tolerance` 클리어 가능 여부
- 필요 딜 대비 초과 투입량
- 상대 효율
- 속성/니케 희소성 보존
- 공격 횟수
- 강한 공격과 작은 조정 공격을 함께 쓰는 유연성

모든 경우를 유지하지 않고 상위 Beam만 유지한다. 현재 기본 Beam width는 24다.

### 3. deterministic local search

사람 눈에 바로 보이는 교환은 CP-SAT에 맡기지 않는다.

최소 보장:
- 같은 속성의 서로 다른 라운드 공격 1:1 swap
- swap 후 도달 라운드가 유지/상승하고, 목표 딜이 증가하거나 같은 목표 딜에서 낭비가 감소하면 즉시 반영
- 여러 pass를 반복해 연쇄 swap 허용

대표 회귀:
- R1 필요 196, R2 필요 200
- 공격 A=216, B=200
- 최종 결과는 `R1 B=200 / R2 A=216`이어야 한다.

### 4. CP-SAT 전역 개선

Beam + local search 결과를 **완성된 실행 가능 hint**로 CP-SAT에 넣는다.

CP-SAT은 다음과 같은 복잡한 재배치를 담당한다.
- 동일 니케가 여러 속성 파티에 걸리는 경우
- 다른 유저가 한 속성을 대신 치면서 희소 파티를 다른 속성에 보존하는 경우
- 여러 라운드와 공격권이 동시에 얽힌 전역 교환

모든 원래 후보는 모델에 남는다. 초기해를 깨는 것이 더 좋으면 자유롭게 재배치한다.

### 5. 최종 sanity swap

CP-SAT 결과를 그대로 출력하지 않고 동일한 deterministic 1:1 swap 검사를 다시 수행한다.

따라서 단순한 `216/200` 역배치는 CP-SAT의 탐색 운에 의존하지 않아야 한다.

## 시간 예산

전체 wall-clock 기본 제한은 30초다.
- 초기 자원 분석/Beam/local search: 빠르게 실행
- CP-SAT 도달 확인: 짧은 예산
- 목표 딜의 큰 개선: 대부분의 남은 시간
- 낭비 정리: 마지막 일부 시간
- 30초가 되면 best-so-far 반환

## 하지 말아야 할 것

- 효율 높은 후보만 남기고 나머지를 삭제하지 않는다.
- 초기 배치를 강제 고정하지 않는다.
- 앞 라운드 오버딜 최소화를 최종보스/마지막 라운드 딜보다 우선하지 않는다.
- CP-SAT 후 greedy 공격을 추가하지 않는다.
- 단순 swap까지 전부 CP-SAT이 우연히 찾기를 기다리지 않는다.
- `OPTIMAL` 증명을 제품의 성공 기준으로 삼지 않는다.

## 자동 회귀

`verify-planner-objectives.mjs`
- 앞 라운드의 약간 큰 오버딜을 감수해서라도 마지막 라운드/최종보스의 큰 딜을 보존하는지 검증한다.

`verify-planner-global-optimization.mjs`
- 설화/아니스 같은 속성 간 전역 교환
- 10억 tolerance 안에서 낮은 낭비 클리어
- R1 216 / R2 200 obvious swap

GitHub Pages 배포 전에 위 테스트가 모두 통과해야 한다.
