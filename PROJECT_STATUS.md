# 세션 인수인계

최종 갱신: 2026-09-07. 이 파일은 진행 기록이며, 고정된 목표·제약은 `AGENTS.md`를 따른다.

## 2026-09-07 전역 최적화 재구성

사용자 검토에서 기존 계산 결과에 다음 문제가 확인됐다.

- R1 작열에서 실제 HP를 약 36억 초과하는 공격을 선택했지만, 약 159.91억 공격으로 10억 이하 잔여 HP를 만들면서 실제 오버딜 0으로 정리할 수 있는 대안이 있었다.
- 설화처럼 동일 니케(예: 아니스)를 여러 속성 파티가 공유할 때, 한 속성에 먼저 써 버리는 선택과 다른 유저가 해당 속성을 대신 담당해 공유 니케를 다른 속성에 남기는 전역 교환을 더 강하게 탐색할 필요가 있었다.
- 기존 엔진은 CP-SAT 핵심 해 뒤에 greedy 추가 공격을 붙였기 때문에 최종 출력 전체가 하나의 전역 최적화 해라는 보장이 없었다.

이에 `planner-cpsat-engine.js`를 다시 구성했다.

- 목적 우선순위는 **최대 도달 라운드 → 마지막 도달 라운드/최종보스 딜 최대 → 그 목표를 유지한 낭비 딜 최소**다.
- 상위 단계가 `OPTIMAL`로 증명되기 전에는 하위 목적 최적화로 넘어가지 않는다. 최대 계산 시간은 한 탐색당 300초다.
- 일반 보스는 잔여 HP 10억 이하를 계획상 클리어로 인정한다. 앞선 라운드에서는 `HP - 10억` 임계치를 넘긴 추가 투입 딜을 계획 낭비로 최소화한다. 목표 라운드는 실제 잔여 HP까지만 유효 딜로 인정하고 실제 HP 초과 딜을 최소화한다.
- 모든 최종 공격 선택은 CP-SAT 변수에서 나오며 기존 reserve + greedy 추가 공격 단계를 제거했다.
- 니케 중복 제약은 유저별 모든 속성 후보를 동시에 묶으므로, 동일 니케를 어느 속성에 남길지 다른 유저 배치와 함께 전역적으로 비교한다.
- `planner-optimizer-ui.js`를 추가했다. GitHub Pages처럼 `crossOriginIsolated`가 false인 환경에서는 최대 4개의 독립 Web Worker를 서로 다른 seed로 병렬 실행하고, 도달 라운드/목표 딜/계획 낭비/실제 오버딜 순으로 가장 좋은 결과를 선택한다. cross-origin isolation이 가능한 환경에서는 엔진 내부 CP-SAT worker를 최대 8개 사용한다.
- 결과 화면에 `OPTIMAL` 증명 여부와 단계별 `OPTIMAL/FEASIBLE/SKIPPED` 상태를 표시한다.

## 회귀 검증

- 기존 `verify-planner-objectives.mjs`는 마지막 도달 라운드/최종보스 딜이 앞 라운드 오버딜보다 우선이라는 목적을 검증한다.
- 신규 `verify-planner-global-optimization.mjs`는 두 사례를 고정한다.
  - 설화의 공유 니케를 수냉에 보존하고 다른 유저가 작열을 담당해야 R1이 클리어되는 전역 교환 사례.
  - R1 작열에 134,146,924,370이 이미 들어간 상태에서 15,990,954,991 공격을 선택해 잔여 HP 703,934,239 / 실제 오버딜 0으로 만드는 사례. 20,304,443,028 공격의 3,609,553,798 오버딜을 선택하면 실패다.
- 현재 대화 환경에서는 저장소 의존성 전체를 로컬 실행할 네트워크가 없어 새 회귀 스크립트의 실제 Node 실행 여부는 GitHub/브라우저 환경에서 추가 확인해야 한다. 문법 수준에서는 새 엔진과 UI 스크립트를 점검했다.

## 구현 및 배포 기록

- 저장소: `https://github.com/mu0767/union.git`, 브랜치 `main`.
- 실제 사용 사이트: `https://mu0767.github.io/union/planner.html`.
- 이번 변경의 주요 커밋: `897dc29`(전역 엔진), `d3be534`/`2bf610b`(5분 병렬 UI), `0e8b5bd`(페이지 로드), `3651747`(회귀 테스트), `f56e31c`(지침 갱신).
- GitHub Pages 반영 여부와 실제 브라우저 회귀 실행은 별도로 확인하고, 확인 전에는 배포 완료로 간주하지 않는다.

## 검증 및 작업 환경

- 빌드: `npm run build:cpsat` (`build-cpsat.mjs`). 생성 자산 `vendor/cpsat/`, `vendor/build/`는 Git 제외, Pages 워크플로에서 생성한다.
- 브라우저 검증: `verify-cpsat-browser.mjs`; `--live`는 위 GitHub Pages에서 실행한다. 현재 스크립트의 기존 90초 대기 한도는 5분 최적화와 맞지 않을 수 있어 후속 정리가 필요하다.
- 이 PC의 Node 대체 경로: `.sites-tools/node.exe`, Python 대체 경로: `.venv/Scripts/python.exe`. 다른 환경에서는 존재 여부를 확인한다.
- `.github/workflows/pages.yml`과 `static.yml` 둘 다 배포를 트리거한다. 한 실행이 취소될 수 있으므로 실제 성공한 커밋과 사이트 반영을 확인한다.
- 수정 완료, 커밋, 푸시, 배포 성공, 운영 사이트 검증을 구분해서 보고한다. 사용자 데이터 변경 없이 가능한 검증을 우선한다.
