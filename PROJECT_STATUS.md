# 세션 인수인계

최종 갱신: 2026-09-08. 이 파일은 진행 기록이며, 고정된 목표·제약은 `AGENTS.md`를 따른다.

## 공개 계정 표시 제거

- 사용자 요청으로 계획 화면의 GitHub 계정 링크와 GitHub API 조회를 제거했다. 빌드 시각은 배포 시 생성하는 `build-info.json`에서 읽는다.
- 프런트엔드 변경과 새 주소 배포 완료: `fd845006-d444-4015-a2d9-00fc181ff033`.
- 사용자 대시보드 조작 후 계정 서브도메인이 `union-raid`로 변경됐다. 새 주소는 `https://union.union-raid.workers.dev`이며 D1과 Worker는 그대로 사용한다. TLS 반영 후 실제 브라우저에서 두 페이지와 API 정상 접속(200), 계정 링크/문자열 제거, 빌드 시각 표시를 확인했다.
- D1 읽기 검증: version 1, 멤버 19명, 완료 공격 0개 유지. 이번 변경에서 공유 데이터를 수정하지 않았다.

## Cloudflare 공유 저장 전환 (배포 완료)

- 사용자 요청으로 Workers Static Assets + D1 구성을 추가했다. `wrangler.jsonc`, `worker/schema.sql`, `build-cloudflare.mjs`, `CLOUDFLARE.md`에 배포 설정과 절차를 마련했다.
- `/api/shared-state`에 딜량 파일·배율·전체 계획·공격 결과·잠금·남은 공격권을 저장한다. 보스 API와 같은 D1 행/version을 사용한다. 기록·배율 변경은 계획 입력에도 반영하고 계산 결과를 무효화한다.
- 공격 기록과 공격권은 원자적으로 저장한다. 오래된 버전은 409로 거절하며 클라이언트는 최신 데이터를 불러온다. 미저장 입력은 로컬 백업한다. 서버에서 니케 중복·공격권 한도·속성·라운드 진행을 검사한다.
- `app.js`와 `planner.js`는 초기 공유 데이터 조회 후 저장을 허용한다. 기존 localStorage는 공유 데이터의 원본으로 자동 업로드하지 않는다. 다른 기기는 새로고침으로 최신 상태를 읽는다.
- 계산 엔진과 최적화 목적함수는 수정하지 않았다. 일반 보스 허용 오차와 목표 우선순위를 유지한다.
- 검증: 실제 SQLite 공유 API 테스트 5개 통과. 로컬 Wrangler/D1와 두 브라우저에서 파일 업로드·공격·공격권·배율·계산 결과 공유, 오래된 저장 거절, WASM crossOriginIsolated 확인. 목적함수 회귀 3개 통과. `wrangler deploy --dry-run` 통과(과거 빌드 잔여 파일을 제거한 정적 파일 52개).
- Cloudflare 로그인 완료. D1 `union-db` 생성 및 실제 ID `f8ec5499-657a-4239-b81b-1333d1a10034` 연결, 원격 schema 적용 완료. 계정의 `union-raid.workers.dev` 서브도메인 등록 완료.
- **2026-09-08 원격 배포 완료.** 사용자 이메일 인증 후 Worker `union` 배포 성공. 버전 ID: `e8e99032-e523-4917-9aca-2610ed9e7693`. 운영 주소: `https://union.union-raid.workers.dev`.
- 운영 검증: 두 브라우저에서 같은 D1 계획/멤버 ID 조회, 기본 19명·완료 공격 0개·배율 전부 1 확인. 첫 운영 접속에서 기본 계획 입력을 D1에 초기화(version 1). 페이지·공유 API 2개·CP-SAT JS/WASM 200, crossOriginIsolated=true, 브라우저 JS 오류 0개. 운영 데이터에 가짜 공격이나 테스트 업로드를 넣지 않았다.
- GitHub 푸시/Pages 재배포는 하지 않았다. 기존 Pages는 이전 배포 상태이며, 공유 기능은 위 Cloudflare 주소를 사용한다.

## 목적함수 수정 및 검증

**기존의 오버딜 우선 고정 문제를 계산 코드에서 수정했다.**

- `planner-cpsat-engine.js`는 도달 라운드를 정한 뒤, 최종보스 딜 또는 마지막 도달 라운드만의 유효 딜을 최대화한다. 해당 목표 딜 하한을 고정한 후 오버딜을 줄인다. 모든 일반 라운드의 딜을 합산하여 주목표로 쓰지 않는다.
- 남은 공격권용 호환 파티 확보와 추가 배치는 유지한다. 화면과 완료 메시지에 `targetDamage`(최종보스 딜 또는 마지막 도달 라운드 유효 딜)를 표시한다.
- `verify-planner-objectives.mjs`: 기존 코드에서 실패하는 사례를 재현한 뒤 수정 코드에서 통과 확인. 앞 라운드 오버딜 50을 허용해 마지막 목표 딜 1,000을 확보하는 조합을 선택한다(오버딜 0인 대안의 목표 딜은 10). 최종보스 도달/미도달 각각 검증한다.
- 추가로 앞 라운드의 허용 HP 부족분을 채우는 것보다 마지막 라운드 딜을 우선하는 사례를 검증한다.
- 기본 데이터 로컬 검증: 57회 사용, 미사용 0, R3 유효 딜 777,747,591,125. 비교용 기존 코드 실행은 746,943,684,860. 시간 제한 탐색의 개별 실행 결과이며 모든 실행에서 동일하다고 보장하지 않는다.
- 목적함수 변경을 배포할 때 실제 운영 페이지의 `targetDamage` 및 화면 표시도 검증하고 아래 배포 기록을 갱신한다.

## 구현 및 배포 기록

- 저장소: `https://github.com/mu0767/union.git`, 브랜치 `main`.
- 현재 공유 사이트: `https://union.union-raid.workers.dev/planner.html`. 아래 Pages 관련 기록은 이전 배포 이력이다. `chatgpt.site` 주소는 사용자의 운영 사이트가 아니다.
- Cloudflare Workers가 정적 파일과 공유 API를 제공하고 브라우저 Worker의 CP-SAT 엔진이 계산한다. `app.js`는 메인 기록 보드이고 계획 계산 엔진이 아니다.
- 마지막 확인된 계산 코드 배포: `442bf31` (남은 공격권을 위한 호환 파티 확보와 추가 배치). 배포 후 기본 데이터에서 57회 사용·미사용 0회·R2 전격 오버딜 0·니케 중복 없음 확인.
- 이는 당시 실행 결과다. 제한 시간 탐색이므로 편성과 딜 결과가 달라질 수 있다. 기본 데이터의 19명 × 3회 = 57회를 모든 입력에 하드코딩하지 않는다.
- 보스별 ±10억 허용과 레벨 공격력 보정 퍼센트 표시는 구현되어 있다.

## 검증 및 작업 환경

- 빌드: `npm run build:cpsat` (`build-cpsat.mjs`). 생성 자산 `vendor/cpsat/`, `vendor/build/`는 Git 제외, Pages 워크플로에서 생성한다.
- 브라우저 검증: `verify-cpsat-browser.mjs`; `--live`는 위 GitHub Pages에서 실행한다. 현재 스크립트는 Windows Chrome 경로와 로컬 `.sites-tools`에 의존한다.
- 이 PC의 Node 대체 경로: `.sites-tools/node.exe`, Python 대체 경로: `.venv/Scripts/python.exe`. 다른 환경에서는 존재 여부를 확인한다.
- `.github/workflows/pages.yml`과 `static.yml` 둘 다 배포를 트리거한다. 한 실행이 취소될 수 있으므로 실제 성공한 커밋과 사이트 반영을 확인한다.
- 수정 완료, 커밋, 푸시, 배포 성공, 운영 사이트 검증을 구분해서 보고한다. 사용자 데이터 변경 없이 가능한 검증을 우선한다.
