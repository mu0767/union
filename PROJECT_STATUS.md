# 세션 인수인계

최종 갱신: 2026-09-07. 고정 목표·제약은 `AGENTS.md`를 따른다.

## 2026-09-07 실전형 anytime 최적화 전환

사용자는 수학적으로 100% 최대값을 증명하는 것보다 실제 레이드에서 충분히 높은 딜과 좋은 자원 배분을 빠르게 얻는 것을 원한다.

현재 정책:
- 전체 R1→R2→R3→최종보스는 계속 하나의 전역 CP-SAT 모델에서 본다.
- 일반 보스는 잔여 HP 10억 이하를 계획상 클리어로 인정한다.
- 유저별 레벨 보정 상대 효율(같은 속성 중앙값 100%)로 잘 치는 속성/파티를 먼저 탐색하도록 hint를 준다.
- hint는 고정이 아니다. 모든 후보를 그대로 남겨 설화의 아니스를 작열 대신 수냉에 남기는 식의 전역 교환을 허용한다.
- 도달 라운드를 최우선으로 하고, 이후 최종보스/마지막 도달 라운드 딜을 높은 구간까지 끌어올린다.
- 허용 오차보다 작은 딜 차이의 완전한 최적성 증명에는 집착하지 않는다.
- 목표 딜 구간을 유지하면서 일반 라운드의 계획 낭비를 정리한다.
- greedy로 최종 공격을 추가하지 않는다. 출력 공격은 모두 CP-SAT 해에서 나온다.
- 기본 최대 60초. GitHub Pages에서는 여러 독립 Web Worker/seed를 병렬 실행해 가장 좋은 전역 해를 선택한다.

## 과거 문제와 회귀 기준

반드시 계속 막아야 하는 사례:
- R1 작열에 134,146,924,370이 이미 들어간 경우 15,990,954,991 공격으로 잔여 HP 703,934,239 / 실제 오버딜 0이 가능한데 20,304,443,028 공격으로 3,609,553,798 오버딜을 만드는 배치를 선호하면 안 된다.
- 설화의 공유 니케를 수냉에 쓰고 다른 유저가 작열을 담당해야 전체 진행이 좋아지는 경우, 작열 우선배정을 고정하면 안 된다.
- FEASIBLE 중간해에서 목표딜/낭비 단계를 SKIPPED하고 수백~수천억 오버딜을 그대로 반환하면 안 된다.

회귀 스크립트:
- `verify-planner-objectives.mjs`
- `verify-planner-global-optimization.mjs`

## 현재 구현

- `planner-cpsat-engine.js`: 효율 기반 good-first hint + 전체 후보 전역 CP-SAT + 목표 딜 구간 + 낭비 정리.
- `planner-optimizer-ui.js`: 기본 최대 60초 병렬 portfolio 탐색. `LEVEL_ATTACK_POWER`를 worker 입력에 전달해 엔진이 화면과 같은 상대 효율 기준을 사용한다.
- 결과 UI는 OPTIMAL 증명 자체보다 “실전 최선해”임을 중심으로 표시한다.
- 실제 사용 사이트: https://mu0767.github.io/union/planner.html

## 검증/배포

GitHub Pages workflow는 CP-SAT 빌드 후 위 두 optimizer 회귀 스크립트를 실행하며 실패하면 배포를 중단한다. 최신 커밋의 workflow 성공과 운영 사이트 반영은 별도로 확인한다.
