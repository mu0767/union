# 프로젝트 작업 지침

이 저장소에서 작업하는 모든 에이전트는 구현·수정·검증 전에 아래 최적화 목표를 확인한다. 이후 사용자의 명시적인 변경 지시가 없는 한 모든 대화와 작업에 적용한다.

## 세션 시작과 인수인계

- 이 파일이 공통 프로젝트 지침의 원본이다. Claude에서는 루트 `CLAUDE.md`를 통해 같은 지침을 읽는다.
- 시작할 때 이 파일과 `PROJECT_STATUS.md`를 읽고, `git status` 및 관련 코드를 확인한다. 상태 문서의 과거 검증 결과를 현재 코드의 보장으로 간주하지 않는다.
- 사용자가 목표나 제약을 정정하면 이 파일을 갱신한다. 진행 상황·미완료 문제·검증·배포 상태는 `PROJECT_STATUS.md`에 기록한다.
- 현재 사용자의 명시적인 지시가 기록된 지침보다 우선한다. 파일에 없는 선호나 승인을 만들어내지 않는다.
- 다른 체크아웃에서도 공유할 수 있도록 지침과 상태 문서를 Git으로 관리한다. 문서 변경만으로 계산 로직까지 수정됐다고 보고하지 않는다.

## 레이드 계획의 최우선 목표

**전체 레이드를 처음부터 끝까지 전역으로 계산해 최종적으로 가능한 딜을 최대화한다. 일반 라운드에서는 실제 공격 오차를 감안해 작은 딜 차이를 같은 수준으로 취급하고, 그 범위 안에서는 자원을 더 효율적으로 쓰는 조합을 선택한다.**

계획은 다음 우선순위로 비교한다.

1. 가능한 한 높은 라운드에 도달한다.
2. 같은 도달 라운드라면 최종보스 딜을 최대화한다. 최종보스 미도달 시 마지막 도달 라운드의 보스별 실제 잔여 HP까지 인정되는 유효 딜 합계를 최대화한다.
3. 단, 2번의 딜 차이가 사용자가 허용한 오차 범위 안이면 사실상 동급으로 취급하고, 그 안에서는 앞선 라운드의 과도한 오버딜과 자원 낭비를 줄인다.
4. 같은 수준의 결과라면 공격권·강한 파티·공유 니케를 더 유용하게 배분하는 조합을 선택한다.

## 반드시 지킬 해석

- 오버딜 최소화 자체가 최우선 목표는 아니며, 더 높은 라운드나 의미 있게 더 큰 최종 딜을 희생해서는 안 된다.
- 반대로 딜 차이가 허용 오차 범위 안인데 한쪽이 수십~수백억의 추가 오버딜을 만든다면 그 해를 선택하지 않는다.
- 모든 라운드의 딜을 단순 합산하는 것으로 마지막 도달 라운드/최종보스 딜 최대화를 대체하지 않는다.
- 공격 횟수 최소화를 독립 목표로 두지 않는다. 사용 가능한 공격권은 전체 레이드 목표에 실제로 도움이 되는 곳에 배치한다.
- 공격 횟수만 늘리기 위해 이미 처치한 보스에 공격을 넣거나, 속성·공격권 한도·사용 니케 중복·라운드 진행 조건을 위반하지 않는다.
- 일반 보스 HP 대비 1,000,000,000(10억) 이하 잔여 HP는 계획상 클리어로 인정한다. 앞선 라운드는 `HP - 10억`을 넘긴 뒤 추가로 사용한 딜을 자원 낭비로 취급한다. 화면의 오버딜은 실제 HP를 초과한 값만 그대로 표시한다.
- 마지막 도달 라운드의 유효 딜은 실제 잔여 HP까지만 인정한다.
- 딜 최대화는 1원 단위의 이론적 최댓값을 고집하지 않는다. 기본적으로 허용 오차 크기의 구간으로 비교하되, 목표 딜 규모가 작으면 구간 폭을 자동으로 더 촘촘하게 줄여 정밀도를 유지한다.
- 한 유저의 니케를 어느 속성 파티에 쓰는지가 다른 유저와 다른 속성 배치에 미치는 기회비용을 전역적으로 비교한다. 예를 들어 동일 니케를 작열에 쓰는 대신 수냉에 남기고 다른 유저가 작열을 담당하는 교환도 같은 모델 안에서 비교해야 한다.
- 핵심 조합을 CP-SAT으로 구한 뒤 greedy로 공격을 덧붙여 전역 최적해를 깨뜨리지 않는다. 최종 출력 공격은 모두 같은 CP-SAT 해에서 선택한다.
- 기본 계산 시간은 최대 30초다. 상위 목적이 제한 시간 내 `OPTIMAL`로 증명되지 않더라도 현재까지 찾은 최고 도달 라운드를 고정한 뒤 목표 딜 최적화와 낭비 정리를 반드시 계속 수행한다. `목표딜 SKIPPED` 또는 `낭비 SKIPPED` 상태의 중간 해를 정상 결과로 반환하지 않는다.
- 전체 계산 시간 중 일정 시간을 낭비 정리에 반드시 예약한다. 목표 딜 탐색이 시간을 모두 소비해 수백억~수천억의 불필요한 오버딜이 남는 결과를 허용하지 않는다.
- 제한 시간 내 `FEASIBLE`만 얻으면 화면에서 최적성 미증명 상태를 명확히 표시한다.
- 브라우저 환경이 WebAssembly 내부 멀티스레드를 지원하면 여러 CP-SAT worker를 사용한다. GitHub Pages처럼 cross-origin isolation이 없는 경우에는 서로 다른 seed의 독립 Web Worker 탐색을 병렬로 수행하고 위 우선순위로 최선 결과를 선택한다.
- 딜 옆 퍼센트는 레벨별 공격력으로 나눈 딜을 같은 속성의 중앙값(100%)과 비교한 비율이다. 보스 HP 대비 비율이 아니다.

## 구현 및 검증 위치

- 운영 페이지: https://mu0767.github.io/union/planner.html
- 계산 경로: `planner.js` → `planner-optimizer-ui.js` → `planner-cpsat-worker.js` → `planner-cpsat-engine.js`.
- 목표에 대한 기준은 이 문서이며, 기존 코드의 목적함수 순서가 올바르다고 가정하지 않는다.
- 회귀 검증에는 **오버딜은 더 크지만 마지막 도달 라운드/최종보스 딜이 의미 있게 더 큰 계획을 선택하는 사례**, **허용 오차 구간 안에서는 더 적은 딜로 앞 라운드를 정리하는 사례**, **공유 니케를 다른 속성에 보존해야 전체 진행이 좋아지는 전역 교환 사례**, **FEASIBLE 상태에서도 목표딜과 낭비 정리를 수행하는 사례**를 포함한다.
- 제한 시간 내 찾은 실행 가능한 해와 최적성이 증명된 해를 구분한다.
- 로컬 수정과 GitHub Pages 배포 여부를 명확히 구분한다.
